apiVersion: v1
kind: Pod
metadata:
  name: mongodb-service
  labels:
    helm.sh/chart: nestjs-app-0.1.0
    app.kubernetes.io/name: mongodb-service
    app.kubernetes.io/instance: mongodb-service
    app.kubernetes.io/part-of: nestjs-app
spec:
  containers:
  - name: test
    image: busybox
    command: ['sh', '-c', 'echo "Testing connection to MongoDB service" && nc -z mongodb-service 27017']
  restartPolicy: Never